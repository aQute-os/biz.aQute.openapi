buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath group: 'biz.aQute', name: 'biz.aQute.openapi.gradle.plugin',
                  version: '1.0.0-SNAPSHOT'
    }
}

apply plugin: 'biz.aQute.openapi'

openapi {
	input			= file( 'petstore.json' )
	output			= file( 'gen-src')
	
	configuration.tags				= "*"
	configuration.typePrefix		= "Generated"
	configuration.baseName			= "OpenAPIBase"
	configuration.packagePrefix		= "aQute.openapi.example.petstore";
}

